package com.example.ticketing.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MongoConfig {
    // Defaults via spring.data.mongodb.uri. Extend with custom converters if needed.
}
