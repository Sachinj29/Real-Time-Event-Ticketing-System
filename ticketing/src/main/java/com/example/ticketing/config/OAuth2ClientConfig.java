package com.example.ticketing.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class OAuth2ClientConfig {
    // Spring Boot auto-configures clients from application.properties.
    // We keep this class to host any custom converters or authorized client repo if needed later.
}
